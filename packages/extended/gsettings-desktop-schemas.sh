package_name=gsettings-desktop-schemas
package_version=3.20.0
tarball_suffix=xz
build_dependencies="glib"

# IMPORTANT NOTE: after re-installing gsettings-desktop-schemas, we need to reintall metacity.sh and metacity-theme.sh (both in hutchr/applications)
#                 usually we don't need to reinstall it, it comes with default axiom image
