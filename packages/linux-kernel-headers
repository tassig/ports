package_name=linux-kernel-headers
package_version=4.6.7
build_dependencies=
iscustombuild=1

custombuild(){
	url=https://cdn.kernel.org/pub/linux/kernel/v4.x/linux-4.7.3.tar.xz
	rm -rf builddir
	mkdir -p builddir   # do everything in builddir for tidiness
	cd builddir
	wget -O archive $url
	tar xvf archive
	rm archive
	cd *   # cd into the package directory
	make INSTALL_HDR_PATH=$installdirectory/$package_name-$package_version/ headers_install
	ln -sv $installdirectory/$package_fullname $installdirectory/$package_name
	cd ../..
	rm -r builddir
}
