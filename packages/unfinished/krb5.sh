wget wget https://kerberos.org/dist/krb5/1.16/krb5-1.16.1.tar.gz

echo "proceed manually from now"

# this is a default build, besides the fact that everything is in "src" instead of the root directory
